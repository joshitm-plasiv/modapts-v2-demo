# Agentic digital-twin demo — LLM-only, POR-driven, multi-tool.
streamlit>=1.32
anthropic>=0.30          # Anthropic provider (live LLM)
google-genai>=1.0        # Gemini provider (unified google-genai SDK)
openpyxl>=3.1            # POR .xlsx ingestion (deterministic)
pdfplumber>=0.11         # POR .pdf ingestion (text + tables)
# The MODAPTS/PMTS engine is the local repo package (top-level `modapts/`), imported directly.
