"""Standalone demo — core logic (agents, governance, judge, panel, sample line).

This package is the demo only. The real, backend-agnostic pieces (the Classifier
agent and the memory contract) live in the top-level `modapts` package and are
imported here.
"""
